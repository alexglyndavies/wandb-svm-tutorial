program: train.py
project: wandb-tutorial

method: grid

metric:
  name: test_accuracy
  goal: maximize

parameters:
  gamma:
    values: [0.05, 0.1, 0.5, 1.0, 5.0, 10.0, 50.0]

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - "--config=configs/default.yaml"